localhost {
    # Backend API
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Health check
    handle /health {
        reverse_proxy backend:8000
    }

    # Frontend (catch-all)
    handle /* {
        reverse_proxy frontend:3000
    }
}
